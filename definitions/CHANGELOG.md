# DEFINITIONS CHANGELOG

Every change to a frozen definition is recorded here with a date and a fork point.
Rows logged before a fork are scored separately from rows logged after. No exceptions —
a definition that changes silently makes every earlier row uninterpretable.

| Date | File | What changed | Why | Fork at row |
|---|---|---|---|---|
| | | | | |
